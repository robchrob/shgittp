# shgittp v0.4.5 Configuration
# Location: ~/.config/shgittp/config

# Repository URL
# Must be SSH (git@...) or HTTPS (https://...)
REPO_URL=""
BRANCH="master"

# Remote Paths
DOTFILES_DIR=".dot"
BACKUP_DIR_PREFIX=".dot-backup"
